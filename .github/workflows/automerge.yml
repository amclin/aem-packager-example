name: Automerge

on:
  check_suite:
    types:
      - completed

jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - name: Automerge Dependabot Updates
        uses: amclin/dependabot-automerge@v0.1.2